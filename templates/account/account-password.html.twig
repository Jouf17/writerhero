{% extends 'base.html.twig' %}

{% block title %}Changer mon mot de passe - WriterHero{% endblock %}

{% block body %}

<div class="container">
    <h1>Changer mon mot de passe</h1>
    <a href="{{ path('account')}}">Retourner à mon compte</a>
    <hr>
    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
    {% endif %}
    {{ form(form) }}
    <hr>
    
</div>
{% endblock %}